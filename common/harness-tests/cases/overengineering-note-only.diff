# expect: PASS
--- /dev/null
+++ b/backend/core/core-order/src/main/kotlin/.../order/domain/service/DiscountPolicy.kt
@@ -0,0 +1,3 @@
+interface DiscountPolicy {
+    fun apply(price: Long): Long
+}
--- /dev/null
+++ b/backend/core/core-order/src/main/kotlin/.../order/domain/service/AbstractDiscountPolicy.kt
@@ -0,0 +1,6 @@
+abstract class AbstractDiscountPolicy : DiscountPolicy {
+    final override fun apply(price: Long): Long = discount(price)
+
+    protected abstract fun discount(price: Long): Long
+}
--- /dev/null
+++ b/backend/core/core-order/src/main/kotlin/.../order/domain/service/RateDiscountPolicy.kt
@@ -0,0 +1,8 @@
+@Component
+class RateDiscountPolicy : AbstractDiscountPolicy() {
+    override fun discount(price: Long): Long = price - price * RATE / 100
+
+    companion object {
+        private const val RATE: Long = 10
+    }
+}
--- /dev/null
+++ b/backend/core/core-order/src/test/kotlin/.../order/domain/service/RateDiscountPolicyTest.kt
@@ -0,0 +1,8 @@
+class RateDiscountPolicyTest {
+
+    @Test
+    fun `정률 할인을 적용한다`() {
+        assertThat(RateDiscountPolicy().apply(10000L)).isEqualTo(9000L)
+    }
+}
