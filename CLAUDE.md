# 프로젝트 규칙

> **이 문서는 에이전트가 읽습니다.** 사용자가 읽을 문서는 [README.md](README.md) 입니다.

코딩 규칙 스킬은 세 모듈로 나뉜다 — 공통 `.claude/skills/`, 백엔드 `backend/.claude/skills/`, 프론트엔드 `frontend/.claude/skills/`. 어떤 규칙이 적용될지는 **고치는 파일의 위치**가 정한다.

리뷰 규칙은 아래 import 로 함께 읽힌다. 작성 기준과 리뷰 기준은 같은 문서에서 관리한다.

문서 규칙: md 는 한 줄에 문장을 이어 쓰다가 150자를 넘기 전에, 문장이 끝나는 지점에서 줄을 끊는다. 줄과 줄 사이는 빈 줄로 띄운다.

문장 중간에서 억지로 자르지 않는다 — 한 문장이 150자를 넘으면 그대로 둔다. 인용(>) 연속 줄은 `>` 한 줄로 구분한다. 표·코드 블록·frontmatter 는 예외다.

## 규칙을 고칠 때는 그 규칙을 말하는 문서를 전부 찾는다

**스킬 하나만 고치면 규칙이 바뀌지 않는다.** 같은 주제를 말하는 문서가 여럿이라, 스킬이 새 규칙을 말해도 README·CONTRACT·노드 문서가 옛 규칙을 말하고 있으면 에이전트는 그쪽을 따른다.

실제로 스킬만 고친 뒤 같은 과제로 구현 테스트를 두 번 돌렸고, 두 번 다 옛 구조가 나왔다. 스킬 밖 문서를 함께 맞춘 뒤에야 바뀌었다.

절차는 셋이다.

1. 바꾼 규칙의 **낱말**로 저장소 전체 md 를 훑는다 — 옛 이름(모듈명·경로·타입명)까지 함께 검색어에 넣는다.

2. 걸린 문서를 **규칙을 말하는 것**과 **예시로 언급만 하는 것**으로 나눈다. 앞의 것은 반드시 고치고, 뒤의 것도 옛 이름이면 고친다.

3. 고친 뒤 같은 낱말로 다시 훑어 **0건**임을 확인한다. 일부러 남긴 것(과거 상태 설명 등)이 있으면 왜 남겼는지 그 자리에 적는다.

## 시작 전에 승인을 받는 작업

아래는 **하기 전에** 사람에게 보고하고 승인을 받는다. 이미 저지른 뒤에는 되돌릴 수 없는 것들이다.

- **외부 API 를 30건 이상 호출하는 작업** — 대상·예상 호출 수·쿼터 영향 셋을 보고한다.
  **실검증도 예외가 아니다.** 승인 전에는 1건으로만 확인한다.

  상세는 `backend/.claude/skills/kotlin-client` 의 "대량 호출은 시작 전에 승인받는다" 절.

`.claude/hooks/warn-bulk-api-calls.sh` 가 "반복 + HTTP 호출" 명령을 감지해 사용자에게 확인 창을 띄운다.
**감지는 보조 수단이다** — 스크립트 안에서 부르는 호출은 명령줄만 봐서는 모른다. 규칙을 지킬 책임은 에이전트에게 있다.

@common/docs/code-review/rules.md
