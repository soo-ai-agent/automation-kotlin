# 초기 구축 명세

> **이 문서는 사용자가 채우고, 에이전트가 읽습니다.** 사용자→에이전트 주문서입니다.

`>` 안내를 보고 빈칸을 채운다. 안내 줄은 지우지 않아도 된다. 다 채우면 커밋하고 아래처럼 지시한다.

```bash
gh workflow run claude-agent.yml -f prompt="@TASK.md" -f graph="api>web>e2e"
```

## 서비스 개요

> 무엇을 만드는지 한두 문장. 예) 개인용 할 일 관리 — 추가·완료 표시·삭제.

(여기에 적는다)

## 도메인

> 데이터 덩어리마다 소제목 + 표. 모르는 칸은 비워 두면 에이전트가 정한다. 소유자가 있는 데이터면 소유자 필드를 반드시 적는다.

### (도메인 이름)

| 필드 | 타입 | 제약 |
|---|---|---|
| id | Long | PK |
| (필드 추가) | | |

## 기능

> 사용자가 하는 행동을 동사로 한 줄씩. 예) 목록 조회 · 추가 · 완료 토글 · 삭제

- (여기에 적는다)

## 완료 기준

> 확인 가능한 문장으로 — E2E 가 이 기준으로 테스트를 만든다. 실패 케이스도 함께 적는다. 예) 제목이 비면 400 을 준다.

- `cd backend && ./gradlew ktlintCheck unitTest` 통과

- `cd frontend && npm run build` 통과

- (여기에 적는다)

---

여기부터는 선택이다. **쓰지 않는 절은 통째로 지운다** — 지우면 에이전트가 알아서 정한다.

## 화면 (선택)

> 페이지별로 보이는 요소. 예) 목록 화면 — 입력창 + 추가 버튼 + 체크박스 목록, 완료는 취소선.

## API (선택)

> 정해진 엔드포인트 형태가 있으면 적는다. 없으면 에이전트가 설계해 CONTRACT.md 에 기록한다.

## 정책 (선택)

> 권한·검증·정렬·페이징처럼 값이 아니라 규칙인 것. 예) 목록은 20개씩 페이징.

## 범위 밖 (선택)

> 이번에 만들지 않는 것. 예) 로그인 · 관리자 화면
